const initialState = {
    user: {},
    isLoading: false,
    isLoggedIn: false,
    userPhone: "",
    verificationStatus: {}
}

export default function (state = { ...initialState }, action) {
    switch (action.type) {
        default:
            return state
    }
}